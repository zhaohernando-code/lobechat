{
  "mode": "local_runtime_service",
  "projectId": "lobechat",
  "sourcePath": "$HOME/codex/projects/lobechat",
  "runtimeTargetPath": "$HOME/codex/projects/lobechat",
  "preservePaths": [
    "data",
    "backups",
    "deploy/.env"
  ],
  "postSyncCommands": [
    "mkdir -p data/postgresql data/redis data/rustfs backups",
    "if [ -f deploy/.env ]; then scripts/lobehubctl.sh config; else echo 'Skipping compose validation: deploy/.env is not present yet.'; fi"
  ],
  "healthChecks": [],
  "runtime": {
    "type": "docker_compose",
    "composeFile": "deploy/docker-compose.yml",
    "envFile": "deploy/.env",
    "localUrl": "http://127.0.0.1:3210",
    "publicUrl": "https://hernando-zhao.cn/chat"
  },
  "notes": [
    "This project is a deployment wrapper for the official LobeHub image, not a fork of upstream source.",
    "Public /chat routing still requires an edge reverse-proxy rule outside this repository."
  ]
}
